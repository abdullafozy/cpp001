#include <iostream>
using namespace std;
#include <string>
int main()
{
    string s;
    cin>>s;
    bool pallindrome=1;
    for(int i=0 ;i<s.size()/2 ;i++){
        if(s[i]!=s[s.size()-1-i]){
            pallindrome=0;
            break;
        }
    }
    if(pallindrome)
    cout<<"yes";
    else
        cout<<"no";


    return 0;
}
