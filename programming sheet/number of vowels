#include <iostream>
using namespace std;
#include<cctype>
#include<string>
bool isvowel(char c){
    c=tolower(c);
    if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') {
        return true;
    } else {
        return false;
    }
// or return (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u');
فبدل ما نقول للكمبيوتر: "لو نتيجة القوس ده طلعت true، اعمل return true.. ولو طلعت false اعمل return false"، إحنا بنختصر خطوة ونقوله: "اعمل return لنتيجة القوس ده مباشرةً أياً كانت!" //
}
int main()
{
    string s;
    getline(cin,s);
    int count=0;
    for(int i=0 ;i<s.size() ;i++){
        if( isvowel(s[i]) )
        count++;
    }
    cout<<count;

    return 0;
}
